This application was developed with Ruby version 2.1.0

Please run to setup the environment:
  bundle install

To run the command line application:
  bundle exec ruby main.rb <xml> <output>

Relative paths are allowed as arguments

To Run Rspec suite:
  bundle exec rspec

Assumptions:
  - List the first 10 works - I am listing the works in order of the work's timestamp in the XML rather than the actual order it appeared in the file
